import 'package:flutter/material.dart';

class AddProductWebview extends StatelessWidget {
  const AddProductWebview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Add Product - webview'));
  }
}
