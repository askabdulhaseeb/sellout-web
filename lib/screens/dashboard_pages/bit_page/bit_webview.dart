import 'package:flutter/material.dart';

class BitWebview extends StatelessWidget {
  const BitWebview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bit Page - webview'));
  }
}
