import 'package:flutter/material.dart';

class BitPage extends StatelessWidget {
  const BitPage({Key? key}) : super(key: key);
  static const String routeName = '/bit';

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
